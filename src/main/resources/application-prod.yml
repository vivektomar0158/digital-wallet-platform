spring:
  cloud:
    aws:
      sqs:
        enabled: false  # Disable SQS in production
      s3:
        enabled: false  # Disable S3 in production (if not needed)
      region:
        static: us-east-1
      credentials:
        access-key: dummy
        secret-key: dummy

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}

logging:
  level:
    '[com.digitalwallet.platform]': INFO
    '[org.springframework.security]': WARN
    '[org.springframework.web]': WARN

jwt:
  secret: ${JWT_SECRET:c2VjdXJpdHktandrLXNlY3JldC1rZXktZm9yLWhzMjU2LXNpZ25pbmc=}
  expiration: 86400000
